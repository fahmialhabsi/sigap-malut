Your role is to activate the automatic software factory capabilities of the SIGAP platform. All explanations, reasoning, and reports MUST be written in Bahasa Indonesia. Programming code must remain in English. Do not respond in English explanations. -------------------------------------------------- SYSTEM CONTEXT The SIGAP repository is a government service platform built using: Backend Node.js Express.js Sequelize ORM PostgreSQL (primary) SQLite (fallback) Frontend React Vite TailwindCSS System Components already exist: workflow engine RBAC middleware OpenAPI generator module generator dashboard aggregation service master-data definitions -------------------------------------------------- MISSION Activate the SIGAP AI SOFTWARE FACTORY so the system can automatically generate service modules from master-data definitions. The goal is to turn the repository into a self-expanding platform that can generate new government service modules automatically. -------------------------------------------------- PHASE 1 — SYSTEM VALIDATION Scan the repository and confirm the following components exist and are functional: backend/ frontend/ master-data/ docs/ modules/ agents/ Verify: workflowEngine.js rbacMiddleware.js moduleGenerator.js generateOpenApi.js dashboardService.js Do NOT modify files yet. Produce a validation summary. -------------------------------------------------- PHASE 2 — MASTER DATA DISCOVERY Scan the directory: master-data/ Detect all service definitions including: FIELDS*\* MODULE*_ WORKFLOW\__ Build an internal map: moduleKey moduleName fields workflow definition roles involved Estimate how many modules can be generated. -------------------------------------------------- PHASE 3 — MODULE GENERATION PIPELINE For each module definition discovered: Generate backend components: Sequelize model Controller Service Route Ensure routes follow: /api/<module-key> Example: /api/sek-adm /api/bkt-kbj -------------------------------------------------- PHASE 4 — FRONTEND GENERATION For each module generate React components: List page Create form Edit form Detail page Follow existing UI conventions. Use TailwindCSS styling. Register pages in router configuration. -------------------------------------------------- PHASE 5 — WORKFLOW INTEGRATION For each module: Generate workflow configuration using workflowEngine. Define states: draft submitted review approved rejected Ensure transitions are validated. Persist workflow state in database. -------------------------------------------------- PHASE 6 — RBAC GENERATION For each module: Insert RBAC permissions into: role_module_permissions Permissions required: read create update delete submit approve Roles must follow existing role codes. -------------------------------------------------- PHASE 7 — DASHBOARD INTEGRATION Extend dashboardService so each generated module contributes statistics: total_records open_workflows approved_items rejected_items Expose aggregated API: /api/dashboard/summary -------------------------------------------------- PHASE 8 — OPENAPI GENERATION Regenerate OpenAPI specification: docs/api/openapi.yaml Include: request schema response schema authentication requirements Ensure each generated module is documented. -------------------------------------------------- PHASE 9 — CONSISTENCY CHECK Validate repository structure: controllers services routes models pages Remove unused imports. Ensure naming conventions are consistent. -------------------------------------------------- PHASE 10 — SOFTWARE FACTORY REPORT After generation is completed produce a report titled: SIGAP AI SOFTWARE FACTORY ACTIVATION REPORT Include: Total modules detected from master-data Total modules generated Backend files created Frontend files created Workflow configurations created RBAC permissions generated OpenAPI paths generated Dashboard integrations Also include: Files modified Files created Files skipped Finally estimate: SIGAP Software Factory readiness score (0–100) -------------------------------------------------- EXECUTION MODE Run generation gradually. Do NOT freeze the repository. Avoid regenerating modules that already exist unless missing components are detected. Prefer incremental generation. -------------------------------------------------- FINAL GOAL Transform the SIGAP platform into a fully operational AI Software Factory capable of generating government service modules automatically from master-data.
