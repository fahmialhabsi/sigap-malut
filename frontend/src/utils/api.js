/**
 * Alias kompatibilitas — jangan tambah impor baru ke sini.
 * Kanonik: `import api from "../services/api"` (atau `./api` dari folder `services/`).
 */
export { default, setAuthToken, ensureAuthApiDefaults } from "../services/api";
